import React from 'react';
import styles from './About.module.css';

const About = () => {
    return (
        <div className={styles.aboutContainer}>
            You are currently on the About page
        </div>
    );
};

export default About;
